# This is a full line comment
    # This is an indented comment

server { # inline comment after directive
    listen 80;    
    
    server_name localhost;    # another inline comment
    
\t \t location / { \t # tabbed line with comment
\t \t \t root /var/www;
\t \t }
}

# Final comment
